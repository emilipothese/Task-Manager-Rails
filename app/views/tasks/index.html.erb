
<h1>To do list</h1>

  <% @tasks.each do |task| %>
    <p><% if task.completed == true %>
      <span>☑️</span>
    <% else%>
      <span>🔲</span>
      <% end%>
      <%= link_to task.title, task_path(task) %></p>
  <% end %>

<hr>
<%= link_to "Add a new task", new_task_path %>
