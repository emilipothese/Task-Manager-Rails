<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<% if @task.completed == true %>
  <p>Cette tâche est faite ! 🎉</p>
<% else%>
  <p>Cette tâche n'est pas encore faite</p>
  <% end%>

<%= link_to "Edit task", edit_task_path %>

<%= link_to "Delete #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<%= link_to "Bakc to list", tasks_path %>
